/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export", // Enables static exports
  reactStrictMode: true,
  // basePath: "/your-repository-name",  // Uncomment and set this if your project is in a sub-path
};

module.exports = nextConfig;
